<%= form_with(model: mycelium, local: true) do |form| %>
  <% if mycelium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mycelium.errors.count, "error") %> prohibited this mycelium from being saved:</h2>

      <ul>
        <% mycelium.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
